// Get player input

// Movement input
o_input.RIGHT = keyboard_check(vk_right);
o_input.LEFT = keyboard_check(vk_left);

// Init menu movement vars
MENU_UP = keyboard_check(vk_up);
MENU_DOWN = keyboard_check(vk_down);
MENU_ENTER = keyboard_check(vk_enter);

// Init pause key
PAUSE = keyboard_check(vk_escape);